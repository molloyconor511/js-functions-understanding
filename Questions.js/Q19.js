// What's the value of "result" when code is ran? Why?

// Result = 10 as function will return value of 2nd argument; in this case num2 = 10

function myFunction(num1, num2, num3) {
  return num2;
}

const result = myFunction(5, 10, 15);

// Happy with same
